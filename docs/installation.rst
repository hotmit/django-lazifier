============
Installation
============

At the command line::

    $ easy_install django-lazifier

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-lazifier
    $ pip install django-lazifier
